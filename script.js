const hamburger = document.getElementsByClassName('hamburger-wrap');
const clicked = document.querySelectorAll('input').clicked;
// const nav-menu = document.getElementsByClassName('hamburger-nav');
const a =document.getElementsByClassName('a');

if(clicked){
    a.className+='hamburger-nav';
    console.log(first);
}